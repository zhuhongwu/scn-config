Spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/scn-work?useUnicode=true&characterEncoding=utf8&serverTimezone=GMT
    username: root
    password: Abcd!234
  jpa:
    database: MySQL
    show-sql: true
    hibernate:
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
## QQ邮箱配置
  mail:
    host: smtp.qq.com #发送邮件服务器
    username: zhwhongwu@qq.com #发送邮件的邮箱地址
    password:  swcdkrezocakhggh #客户端授权码，不是邮箱密码，这个在qq邮箱设置里面自动生成的
    properties.mail.smtp.port: 465 #端口号465或587
    from: zhwhongwu@qq.com # 发送邮件的地址，和上面username一致
    properties.mail.smtp.starttls.enable: true
    properties.mail.smtp.starttls.required: true
    properties.mail.smtp.ssl.enable: true
    default-encoding: utf-8